type Skill {
  id: ID!
  name: String!
  description: String
  progressEntries: [ProgressEntry]
}

type ProgressEntry {
  id: ID!
  date: String!
  level: Int!
  notes: String
}

type Query {
  getSkills: [Skill]
}

type Mutation {
  addSkill(name: String!, description: String): Skill
}